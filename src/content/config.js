/*import { defineCollection, z } from 'astro:content';


// Definir la colección de poemas
const poemas = defineCollection({
  schema: z.object({
    slug: z.string(),
    titulo: z.string(),
    autor: z.string(),
    fecha: z.string(),
    imagen: z.string(),
    fondo: z.string(),
    texto: z.string(),
    tags: z.array(z.string()),
  })
});


const poemarios =defineCollection({
  schema: z.object({
    slug: z.string(),
    nombre: z.string(),
    imagen: z.string(),
    rotate: z.string(),
    poemas: z.array(z.string())
  })
});

export const collections = { poemas, poemarios };*/